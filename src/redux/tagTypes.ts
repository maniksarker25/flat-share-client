export enum tagTypes {
  users = "users",
  flats = "flats",
  bookingRequests = "bookingRequests",
  profile = "profile",
}

export const tagTypeList = [tagTypes.users, tagTypes.flats, tagTypes.profile];
